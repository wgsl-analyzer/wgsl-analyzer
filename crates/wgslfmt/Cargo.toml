[package]
name = "wgslfmt"
version = "0.1.0"
description = "Tool to find and fix WGSL formatting issues"
repository = "https://github.com/wgsl-analyzer/wgsl-analyzer"
readme = "README.md"
license = "Apache-2.0 OR MIT"
# build = "build.rs"
categories = ["development-tools"]
edition = "2021"

[dependencies]
wgsl-formatter = { path = "../wgsl_formatter", version = "0.0.0" }
anyhow = "1.0.95"
lexopt = "0.3.0"
prettydiff = "0.8.0"
