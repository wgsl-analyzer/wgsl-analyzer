# Snapshot Tests

This directory contains a collection of *descriptive* example tests,
that should cover as many edge cases as possible.
These are in addition to the ones at `wgsl_formatter/src/tests/descriptive`
