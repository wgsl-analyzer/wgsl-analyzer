feat: resolve imports for completions, go-to-definition, and type checking

- Move import-to-file resolution logic from ide to hir_def::resolver
- Add database parameter to Resolver::resolve() and process_all_names()
- When encountering an ImportStatement, resolve it to the target file,
  find the matching item, and return the correct ScopeDef/ResolveKind
- Completions now show imported functions with correct icons and details
- Go-to-definition and type inference now work across file boundaries
